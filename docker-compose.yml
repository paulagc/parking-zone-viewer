services:
  api:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: parking-zone-viewer
    ports:
      - "8080:80"
    volumes:
      - ./:/var/www/html
    restart: unless-stopped
